<?php
include 'header.php';
  echo $table;
  // Displays the table
  // Because we include this file in the controller
  // We have acces to the table variable in that file because it has become one file
  // We can echo it

  echo $selectBox;

include 'footer.php';
?>
